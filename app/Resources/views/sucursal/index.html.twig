{% extends 'base.html.twig' %}

{% block header %}
Sucursales
{% endblock %}

{% block header_description %}
{% endblock %}

{% block body %}

<form class="form-horizontal" action="{{path('sucursal_index')}}" method="GET">
  <div class="row"> 
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title"></h3>
          <div class="box-tools">
            <div class="input-group input-group-sm" style="width: 500px;">
              <input id="btn-input" type="text" class="form-control input-sm" 
                 name="texto" placeholder="Buscar por sucursal" value="{{texto}}"/>

              <span class="input-group-btn">
                  <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
                  <a type="button" class="btn btn-primary btn-md" href="{{ path('sucursal_new') }}"><i class="fa fa-plus"></i> Nuevo</a>
              </span>
            </div>
          </div>
        </div>

        <!-- /.box-header -->
        <div class="box-body table-responsive no-padding">
          <table class="table table-hover">
            <tr>
                <th>Nombre</th>
                <th>Localidad</th>
                <th>Teléfono</th>
                <th>Dirección</th>
                <th width="10%">Estado</th>
                <th width="10%">Acciones</th>
            </tr>
            <tbody>
            {% for sucursal in pagination %}
                <tr>
                    <td>{{ sucursal.nombre }}</td>
                    <td>{{ sucursal.localidad }}</td>
                    <td>{{ sucursal.telefono }}</td>
                    <td>{{ sucursal.direccion }}</td>
                    <td>{% if sucursal.activo %}
                        <span class="label label-success">Activo</span>
                    {% else %}
                        <span class="label label-danger">Inactivo</span>
                    {% endif %}
                    </td>  
                    <td>
                        <a href="{{ path('sucursal_show', { 'id': sucursal.Id }) }}"><span class="glyphicon glyphicon-eye-open"></span>
                        </a>
                        <a href="{{ path('sucursal_edit', { 'id': sucursal.Id }) }}"><span class="glyphicon glyphicon-edit"></span>
                        </a>
                        {% if sucursal.activo %}
                          <a href="{{ path('sucursal_delete', { 'id': sucursal.Id }) }}"><span class="glyphicon glyphicon-remove-sign"></span>
                          </a>
                        {% else %}
                          <a href="{{ path('sucursal_delete', { 'id': sucursal.Id }) }}"><span class="glyphicon glyphicon-ok-circle"></span>
                        </a>

                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</form>
{% endblock %}

{% block paginador %}
    {{ knp_pagination_render(pagination) }}
{% endblock %}